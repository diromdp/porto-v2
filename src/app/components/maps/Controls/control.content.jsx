import { t } from "intlayer";

const content = {
    key: "control-content",
    content: {
        basemap: t({
            en: "Basemap",
            id: "Basemap",
        }),
    },
};

export default content;
